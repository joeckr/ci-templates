[tools]
actionlint = "latest"
hk = "latest"

[tasks."hooks:install"]
description = "Install Git hooks using hk"
run = "hk install"

[tasks."hooks:run"]
description = "Run hk hooks across all files"
run = "hk run --all"
